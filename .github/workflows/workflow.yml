name: pipeline
on:
  push:
    branches:
      - master

jobs:
  build:
    - uses: actions/setup-java@v1
      with:
        java-version: '1.8'

    - uses: actions/checkout@v1
      with:
        fetch-depth: 1
